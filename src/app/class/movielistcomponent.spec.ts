import { MovieListComponent } from './movielistcomponent';

describe('MovieListComponent', () => {
  it('should create an instance', () => {
    expect(new MovieListComponent()).toBeTruthy();
  });
});
